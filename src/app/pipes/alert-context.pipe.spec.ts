import { AlertContextPipe } from './alert-context.pipe';

describe('AlertContextPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertContextPipe();
    expect(pipe).toBeTruthy();
  });
});
