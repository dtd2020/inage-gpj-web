import { ComplainerTypePipe } from './complainer-type.pipe';

describe('ComplainerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ComplainerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
